# DSP React (Clean) + components/ (tiles, sections, embeds) — FIX

This package fixes:
- JSX files renamed to `.jsx` to avoid esbuild loader errors
- `MarkdownBlock.jsx` regex rewritten to avoid unterminated regex

## Run locally
```bash
npm install
npm run dev
```

## Build / preview
```bash
npm run build
npm run preview
```